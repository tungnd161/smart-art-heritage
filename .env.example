# Public Supabase project URL and publishable/anon key. Never add service-role keys here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only key for AI routes. Set this in Vercel, never expose it to the browser.
GEMINI_API_KEY=